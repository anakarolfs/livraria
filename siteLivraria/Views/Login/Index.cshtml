@model LoginModel

@{
    Layout = "_LayoutLogin";
    ViewData["Title"] = "Login";
}

<head>
    <meta charset="utf-8" />
    <link rel="stylesheet" type="text/css" href="~/css/perfil.css" />
</head>

@if(TempData["MensagemErro"] != null)
    {
<div class="alert alert-danger" role="alert">
    <button type="button" class="btn btn-danger btn-sm close-alert" aria-label="Close">X</button>
    @TempData["MensagemErro"]
</div>
    }

<style>
    /*

        body{
            background-color: rgb(273, 136, 118);
        }

        .flex-container {
            display: flex;
            justify-content: center ;
        }

        .container {
            display: flex;
            margin: 7%;
            width: 80%;
            background-color: #ffff;
            box-shadow: 0 0 20px;

            border-radius: 10px;
        }

        .login{
            display: flex;
            justify-content: center;
            align-items: center;
            width: 40%;

        }

        .img {
            width: 55%;
            height: 67vh;

            background: url("C:/Users/anafe/OneDrive/Área de Trabalho/login teste/images/logoLivraria.png") transparent no-repeat bottom center scroll;
        }

        /*
        media only screen and (max-width: 1200px) {
            .container {
                margin: 7%;
                width: 80%;
                height: 80vh;
                background-color: #ffff;
                justify-content: center ;
            }

            .img {
                width: 100%;
                height: 24vh;
                background: url("C:/Users/anafe/OneDrive/Área de Trabalho/login teste/images/logoLivrariaPeq.png") transparent no-repeat bottom center scroll;
            }
        }
        */
</style>



    <div class="flex-container">
     
        <div class="img">
            <!-- <img class="img" > -->
        </div>

        <div class="login">

            <form asp-controller="Login" asp-action="Entrar" method="post">

                <div class="form-group">
                    <label for="login">Login</label>
                    <input type="text" asp-for="login" class="form-control" id="login" placeholder="Seu email">
                    @Html.ValidationMessageFor(x => x.login)
                </div>

                <div class="form-group">
                    <label for="senha">Senha</label>
                    <input type="password" asp-for="senha" class="form-control" id="senha" placeholder="Senha">
                    @Html.ValidationMessageFor(x => x.senha)
                </div>

                <button type="submit" class="btn btn-primary">Entrar</button>

            </form>

        </div>

    </div>

